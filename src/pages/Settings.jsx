import React from 'react';

function Settings() {
  return <p style={{ margin: 0, lineHeight: 1.7 }}>These are dashboard settings and show nested routing with a shared parent layout.</p>;
}

export default Settings;
